import type { AuthMutationsResolvers } from './../../types.generated';
export const AuthMutations: AuthMutationsResolvers = {
  register: ({ password, email }) => {
    // TODO register user
    return "";
  },
  login: ({ password, email }) => {
    // TODO login user
    return "";
  },
};